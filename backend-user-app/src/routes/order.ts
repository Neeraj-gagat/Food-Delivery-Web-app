import { Router } from "express";

const router = Router();

router.post("/",(req, res) => {

})

router.get("/",(req, res) => {
    
})

router.get("/:orderId",(req, res) => {
    
})

export const orderRouter = router;